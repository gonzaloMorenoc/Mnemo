# Bloque C · C3 — PDF del certificado — Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Descargar el Release Assurance Certificate en PDF de un clic, con un pulido ligero de marca Mnemo.

**Architecture:** T1 añade `render_pdf` (xhtml2pdf, reusa `render_html`) + pulido del CSS. T2 expone `GET /v2/certificates/{id}/pdf`. T3 añade el botón "Descargar PDF" en el frontend.

**Tech Stack:** Python/FastAPI/pytest (backend); xhtml2pdf (pure-python); Next.js/TS/vitest (frontend).

## Global Constraints

- **xhtml2pdf pure-python** (sin pango/cairo); el PDF **reusa `render_html`** (HTML y PDF no divergen). CSS compatible con xhtml2pdf: **sin flex/grid/svg**.
- La **firma Ed25519** debe quedar en el PDF; generación **local** (on-premise).
- El endpoint `/pdf` clona la auth del `/html` (`Depends(get_current_user)` + `service.get`).
- Backend `python3 -m pytest -m "not integration"`; frontend `npm test` + `tsc` en `frontend/`.
- Commits terminando con `Claude-Session: https://claude.ai/code/session_0198KfgRWvAM8BhiVz24uTok`.

---

## Task 1: `render_pdf` + pulido del CSS de `render_html`

**Files:** Modify `requirements.txt`, `src/certify/render.py`; Test `tests/test_certify_render.py`.

**Interfaces:** Produces — `render_pdf(cert: Dict[str, Any], signature: str) -> bytes`.

- [ ] **Step 1: Add the dependency.** En `requirements.txt`, añadir una línea: `xhtml2pdf==0.2.16`.

- [ ] **Step 2: Write the failing test** — añade a `tests/test_certify_render.py` (usa un `cert` dict de ejemplo como los tests existentes de ese archivo; si hay un fixture/helper de cert reúsalo):

```python
def test_render_pdf_returns_pdf_bytes():
    from src.certify.render import render_pdf
    cert = {
        "verdict": "apto", "risk_score": 12,
        "identity": {"project": "demo", "commit_sha": "abc123", "run_id": "r1",
                     "created_at": "2026-06-27", "mnemo_version": "1.0", "model_version": "m1"},
        "breakdown": {"real": 1}, "evidence": [], "self_eval": {},
    }
    out = render_pdf(cert, "SIG==")
    assert isinstance(out, bytes)
    assert out[:5] == b"%PDF-"

def test_render_html_pulido_mantiene_contenido_y_marca():
    from src.certify.render import render_html
    html = render_html({"verdict": "apto", "risk_score": 12, "identity": {}, "breakdown": {},
                        "evidence": [], "self_eval": {}}, "SIG==")
    assert "Mnemo" in html
    assert "apto" in html
    assert "SIG==" in html  # la firma
```

- [ ] **Step 3: Run, expect FAIL** (`render_pdf` no existe).

Run: `python3 -m pytest tests/test_certify_render.py -q`

- [ ] **Step 4: Add `render_pdf`** to `src/certify/render.py` (top imports + function at the end):

```python
from io import BytesIO
# ... (existing imports)

def render_pdf(cert: Dict[str, Any], signature: str) -> bytes:
    from xhtml2pdf import pisa  # import local: solo se carga al generar PDF
    html = render_html(cert, signature)
    buffer = BytesIO()
    status = pisa.CreatePDF(html, dest=buffer)
    if status.err:
        raise RuntimeError("No se pudo generar el PDF del certificado")
    return buffer.getvalue()
```

- [ ] **Step 5: Polish `render_html`'s CSS.** En `render_html`, sustituir el `<head>` actual por uno con un `<style>` compatible con xhtml2pdf, y añadir un encabezado de marca. Cambiar SOLO la parte del head + el h1; mantener TODO el resto del contenido (identidad, desglose, auto-eval, evidencia, firma, disclaimer). El bloque a usar:

```python
        "<!doctype html><html lang='es'><head><meta charset='utf-8'>"
        "<title>Release Assurance Certificate</title>"
        "<style>"
        "@page { margin: 1.8cm; }"
        "body { font-family: Helvetica, Arial, sans-serif; color: #24292f; font-size: 11pt; }"
        ".brand { font-size: 9pt; letter-spacing: 2px; color: #57606a; text-transform: uppercase; }"
        "h1 { font-size: 18pt; margin: 2px 0 10px 0; }"
        "h2 { font-size: 13pt; border-bottom: 1px solid #d0d7de; padding-bottom: 3px; margin-top: 18px; }"
        ".verdict { font-size: 15pt; font-weight: bold; }"
        "table { border-collapse: collapse; width: 100%; font-size: 10pt; }"
        "th, td { border: 1px solid #d0d7de; padding: 4px 6px; text-align: left; }"
        "code { font-family: Courier, monospace; font-size: 10pt; }"
        "pre { white-space: pre-wrap; font-size: 9pt; background: #f6f8fa; padding: 6px; }"
        "</style></head><body>"
        "<p class='brand'>Mnemo &middot; Release Assurance</p>"
        "<h1>Release Assurance Certificate</h1>"
```

Y cambiar la línea del verdict para usar la clase `.verdict` (mantener el color):

```python
        f"<p><strong>Evaluación del motor:</strong> <span class='verdict' style='color:{color}'>{_e(cert.get('verdict'))}</span>"
        f" &middot; <strong>Risk score:</strong> {_e(cert.get('risk_score'))}</p>"
```

(El resto del `return` de `render_html` se mantiene igual.)

- [ ] **Step 6: Run, expect PASS** — `python3 -m pytest tests/test_certify_render.py -q` → PASS. Then full `python3 -m pytest -m "not integration" -q` → green.

- [ ] **Step 7: Commit**

```bash
git add requirements.txt src/certify/render.py tests/test_certify_render.py
git commit -m "feat(certify): render_pdf (xhtml2pdf) + pulido de marca Mnemo en el certificado

Claude-Session: https://claude.ai/code/session_0198KfgRWvAM8BhiVz24uTok"
```

---

## Task 2: Endpoint `GET /v2/certificates/{run_id}/pdf`

**Files:** Modify `src/api_v2.py`; Test `tests/test_api_v2_certificates.py`.

**Interfaces:** Consumes — `render_pdf` (T1). Produces — `GET /v2/certificates/{run_id}/pdf`.

- [ ] **Step 1: Write the failing test** — añade a `tests/test_api_v2_certificates.py` (sigue el patrón de los tests del `/html` o `/{run_id}` de ese archivo: el mismo `TestClient`, los mismos overrides de auth/service). Mockear `service.get` para devolver un cert con `canonical_json` + `signature`:

```python
def test_certificate_pdf_endpoint_returns_pdf(client, ...):  # reusa las fixtures del archivo
    # con un cert existente (service.get devuelve canonical_json+signature)
    resp = client.get("/v2/certificates/r1/pdf")
    assert resp.status_code == 200
    assert resp.headers["content-type"] == "application/pdf"
    assert resp.headers["content-disposition"].startswith("attachment")
    assert resp.content[:5] == b"%PDF-"

def test_certificate_pdf_404_when_missing(client, ...):
    # service.get devuelve None
    resp = client.get("/v2/certificates/nope/pdf")
    assert resp.status_code == 404
```

(Mira cómo el test del `/html` resuelve el cliente/auth/service y replícalo — mismo `app.dependency_overrides`.)

- [ ] **Step 2: Run, expect FAIL** (404/route no existe).

Run: `python3 -m pytest tests/test_api_v2_certificates.py -q`

- [ ] **Step 3: Implement the endpoint.** En `src/api_v2.py`:
  1. Cambiar el import: `from fastapi.responses import HTMLResponse, Response`.
  2. Añadir tras `get_certificate_html_v2` (después de su `return`, ~línea 793):

```python
@router.get("/certificates/{run_id}/pdf")
def get_certificate_pdf_v2(
    run_id: str,
    user: AuthenticatedUser = Depends(get_current_user),
    service: CertificateService = Depends(get_certificate_service),
) -> Response:
    try:
        cert = service.get(user_id=user.user_id, run_id=run_id)
    except psycopg.Error as exc:
        raise HTTPException(status_code=502, detail="Database error") from exc
    if cert is None:
        raise HTTPException(status_code=404, detail="certificate not found")
    try:
        pdf = render_pdf(cert["canonical_json"], cert["signature"])
    except Exception as exc:  # noqa: BLE001 — generación de PDF
        raise HTTPException(status_code=500, detail="No se pudo generar el PDF") from exc
    return Response(
        content=pdf,
        media_type="application/pdf",
        headers={"Content-Disposition": f'attachment; filename="certificate-{run_id}.pdf"'},
    )
```

  3. Asegurar el import de `render_pdf`: en la línea `from src.certify.render import render_html`, dejarla como `from src.certify.render import render_html, render_pdf`.

- [ ] **Step 4: Run, expect PASS** — `python3 -m pytest tests/test_api_v2_certificates.py -q` → PASS. Then full `python3 -m pytest -m "not integration" -q` → green.

- [ ] **Step 5: Commit**

```bash
git add src/api_v2.py tests/test_api_v2_certificates.py
git commit -m "feat(api): GET /v2/certificates/{id}/pdf (descarga del certificado en PDF)

Claude-Session: https://claude.ai/code/session_0198KfgRWvAM8BhiVz24uTok"
```

---

## Task 3: Botón "Descargar PDF" en el frontend

**Files:** Modify `frontend/src/lib/api/endpoints.ts`, `frontend/src/components/autopilot/CertificateCard.tsx`; Test `frontend/src/components/autopilot/__tests__/CertificateCard.test.tsx`.

**Interfaces:** Produces — `getCertificatePdf(token: string, runId: string): Promise<Blob>`.

- [ ] **Step 1: Add the client helper** — en `frontend/src/lib/api/endpoints.ts` (NO uses `apiRequest`: parsea text→JSON; necesitamos el blob crudo). Añadir:

```ts
export async function getCertificatePdf(token: string, runId: string): Promise<Blob> {
  const res = await fetch(`/api/v2/certificates/${encodeURIComponent(runId)}/pdf`, {
    method: "GET",
    headers: { Authorization: `Bearer ${token}` },
    cache: "no-store",
  });
  if (!res.ok) throw new Error(`Descarga fallida (${res.status})`);
  return res.blob();
}
```

- [ ] **Step 2: Write the failing test** — `frontend/src/components/autopilot/__tests__/CertificateCard.test.tsx` (mirror `ActionsPanel.test.tsx`). Necesita mockear `getCertificate` (para que haya cert y el botón aparezca), `getCertificatePdf`, `URL.createObjectURL`/`revokeObjectURL`, y el `click` del ancla:

```tsx
// @vitest-environment jsdom
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";

vi.mock("@/components/providers/auth-provider", () => ({ useAuth: () => ({ accessToken: "tok" }) }));
vi.mock("sonner", () => ({ toast: { success: vi.fn(), error: vi.fn() } }));
vi.mock("@/lib/api/endpoints", () => ({
  getCertificate: vi.fn(), generateCertificate: vi.fn(), getCertificatePdf: vi.fn(),
}));

import { getCertificate, getCertificatePdf } from "@/lib/api/endpoints";
import { toast } from "sonner";
import { CertificateCard } from "@/components/autopilot/CertificateCard";

afterEach(() => vi.clearAllMocks());
function renderWithClient(ui: React.ReactNode) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={client}>{ui}</QueryClientProvider>);
}

describe("CertificateCard PDF download", () => {
  it("descarga el PDF al pulsar el botón", async () => {
    (getCertificate as ReturnType<typeof vi.fn>).mockResolvedValue({
      verdict: "apto", risk_score: 12, signature: "SIGSIGSIGSIGSIGSIGSIGSIGSIGSIGSIG" });
    (getCertificatePdf as ReturnType<typeof vi.fn>).mockResolvedValue(new Blob(["%PDF-"], { type: "application/pdf" }));
    const createUrl = vi.fn(() => "blob:x"); const revoke = vi.fn();
    Object.assign(URL, { createObjectURL: createUrl, revokeObjectURL: revoke });
    const click = vi.spyOn(HTMLAnchorElement.prototype, "click").mockImplementation(() => {});

    renderWithClient(<CertificateCard runId="r1" />);
    const btn = await screen.findByRole("button", { name: /descargar pdf/i });
    fireEvent.click(btn);
    await waitFor(() => expect(getCertificatePdf).toHaveBeenCalledWith("tok", "r1"));
    await waitFor(() => expect(click).toHaveBeenCalled());
  });

  it("muestra toast de error si la descarga falla", async () => {
    (getCertificate as ReturnType<typeof vi.fn>).mockResolvedValue({
      verdict: "apto", risk_score: 12, signature: "SIGSIGSIGSIGSIGSIGSIGSIGSIGSIGSIG" });
    (getCertificatePdf as ReturnType<typeof vi.fn>).mockRejectedValue(new Error("boom"));
    renderWithClient(<CertificateCard runId="r1" />);
    const btn = await screen.findByRole("button", { name: /descargar pdf/i });
    fireEvent.click(btn);
    await waitFor(() => expect(toast.error).toHaveBeenCalled());
  });
});
```

- [ ] **Step 3: Run, expect FAIL** (no hay botón "Descargar PDF").

Run (from `frontend/`): `npm test -- CertificateCard`

- [ ] **Step 4: Add the button** to `CertificateCard.tsx` — import `getCertificatePdf`; añadir un handler y un `<Button>` que solo se renderiza cuando hay `cert`. El handler:

```tsx
  async function handleDownloadPdf() {
    try {
      const blob = await getCertificatePdf(accessToken!, runId);
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = `certificate-${runId}.pdf`;
      a.click();
      URL.revokeObjectURL(url);
    } catch {
      toast.error("No se pudo descargar el PDF.");
    }
  }
```

Y en el bloque `{cert ? (...) : (...)}`, dentro de la rama con `cert`, añadir junto a la firma:

```tsx
          <Button size="sm" variant="outline" onClick={handleDownloadPdf}>Descargar PDF</Button>
```

(`Button` y `toast` ya están importados en el componente.)

- [ ] **Step 5: Run, expect PASS** — `npm test -- CertificateCard` → PASS; full `npm test` → green; `tsc --noEmit` (o el typecheck) limpio.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/lib/api/endpoints.ts frontend/src/components/autopilot/CertificateCard.tsx frontend/src/components/autopilot/__tests__/CertificateCard.test.tsx
git commit -m "feat(ui): botón Descargar PDF en CertificateCard

Claude-Session: https://claude.ai/code/session_0198KfgRWvAM8BhiVz24uTok"
```

---

## Notas de cierre

- **Una sola fuente:** `render_pdf` llama a `render_html` — el pulido del CSS (T1) beneficia ambos; no hay plantilla duplicada.
- **El blob necesita el token:** por eso `getCertificatePdf` hace `fetch` directo (con `Authorization`), no `apiRequest` (que parsea JSON). Path relativo `/api/v2/...`, igual que `apiRequest`.
- **Verde en cada fase:** backend pytest (T1, T2); frontend npm test + tsc (T3).
- **Fuera de alcance:** C4 (guion/A-B), rediseño completo del cert, branding configurable, Bloque D.
